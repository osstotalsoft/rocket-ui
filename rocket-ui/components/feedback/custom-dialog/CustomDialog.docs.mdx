---
description: 'CustomDialog component.'
labels: ['mui', 'react', 'custom-dialog']
---

import CustomDialog from './CustomDialog'
import { title, text } from './mocks'

## Basic DialogDisplay component

Basic example that renders a custom-dialog

### Component usage

```js
<CustomDialog id='basic' title={title} content={text} />
```

### Use this live example to interact with CustomDialog component

Live example:

```js live
<CustomDialog id='basic' title={title} content={text} />
```
