---
description: 'SideMenu component.'
labels: ['mui', 'react', 'side-menu']
---

import { CloudDownload, OpenInBrowser } from '@mui/icons-material'
import { ListItemIcon, MenuItem, MenuList } from '@mui/material'
import SideMenu from './SideMenu'
import Typography from '@totalsoft_oss/rocket-ui.components.data-display.typography'

## Basic SideMenu component

Basic example that renders a side-menu

### Component usage

```js
<SideMenu
  content={
    <MenuList>
      <MenuItem disableGutters dense>
        <ListItemIcon>
          <OpenInBrowser />
        </ListItemIcon>
        <Typography>{'Load template'}</Typography>
      </MenuItem>
      <MenuItem disableGutters dense>
        <ListItemIcon>
          <CloudDownload />
        </ListItemIcon>
        <Typography>{'Export Yaml'}</Typography>
      </MenuItem>
    </MenuList>
  }
/>
```

### Use this live example to interact with SideMenu component

Modify the text to see it change live:

```js live
<SideMenu
  content={
    <MenuList>
      <MenuItem disableGutters dense>
        <ListItemIcon>
          <OpenInBrowser />
        </ListItemIcon>
        <Typography>{'Load template'}</Typography>
      </MenuItem>
      <MenuItem disableGutters dense>
        <ListItemIcon>
          <CloudDownload />
        </ListItemIcon>
        <Typography>{'Export Yaml'}</Typography>
      </MenuItem>
    </MenuList>
  }
/>
```
