---
description: 'DialogDisplay component.'
labels: ['mui', 'react', 'dialog-display']
---

import DialogDisplay from './DialogDisplay'
import { text, title } from './mocks'

## Basic DialogDisplay component

Basic example that renders a dialog-display

### Component usage

```js
<DialogDisplay id='basic' title={title} content={text} open={false} />
```

### Use this live example to interact with DialogDisplay component

Live example:

```js live
<DialogDisplay id='basic' title={title} content={text} open={false} />
```
