---
description: 'LinearProgress component.'
labels: ['mui', 'react', 'linear-progress']
---

import LinearProgress from './LinearProgress'

## Basic LinearProgress component

Basic example that renders a linear-progress

### Component usage

```js
<LinearProgress color={'primary'} />
```

### Use this live example to interact with LinearProgress component

Live example:

```js live
<LinearProgress color={'primary'} />
```
