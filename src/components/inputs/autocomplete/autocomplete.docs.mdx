---
description: 'Autocomplete component.'
labels: ['mui', 'react', 'autocomplete']
---

import Autocomplete from './Autocomplete'

## Basic Autocomplete component

Basic example that renders an autocomplete

### Component usage

```js
<Autocomplete required label='Required' value={requiredValue} onChange={setRequiredValue} options={options} />
```

### Use this live example to interact with Autocomplete component

Live example:

```js live
<Autocomplete required label='Required' onChange={() => {}} />
```
