---
description: 'DynamicInput component.'
labels: ['mui', 'react', 'dynamic-input']
---

import DynamicInput from './DynamicInput'

## Basic DynamicInput component

Basic example that renders a dynamic-input

### Component usage

```js
<DynamicInput controlType={'BOOL'} value={false} onChange={() => {}} />
```

### Use this live example to interact with DynamicInput component

Live example:

```js live
<DynamicInput controlType={'BOOL'} value={false} onChange={() => {}} />
```
